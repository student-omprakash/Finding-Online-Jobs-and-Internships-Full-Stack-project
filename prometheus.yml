global:
  scrape_interval: 5s         # Scrape targets every 5 seconds for fast updating metrics
  evaluation_interval: 5s

scrape_configs:
  - job_name: 'careernest-backend'
    metrics_path: '/metrics'
    static_configs:
      - targets: ['backend:5005'] # Refers to backend service defined in docker-compose.yml
